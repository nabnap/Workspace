#pragma once
#include <windows.h>
// Standard Sketcher element type identifiers
enum class ElementColor {
	BLACK = RGB(0, 0, 0), RED = RGB(255, 0, 0), BLUE =
	RGB(0, 0, 255), GREEN = RGB(0, 255, 0)};